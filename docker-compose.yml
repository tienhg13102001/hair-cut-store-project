services:
  postgres:
    image: postgres:17-alpine
    container_name: haircut-postgres
    environment:
      POSTGRES_DB: haircut
      POSTGRES_USER: haircut
      POSTGRES_PASSWORD: haircut123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: